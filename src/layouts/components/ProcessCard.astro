---
import ImageMod from "@/layouts/components/ImageMod.astro";
interface Props {
  logo: string;
  title: string;
  content: string;
}
const { logo, title, content } = Astro.props;
---

<div
  class="p-8 xl:px-10 xl:py-11 rounded-2xl border border-border bg-body flex flex-col items-center text-center"
>
  <div class="mb-6 size-14 rounded-xl bg-secondary grid place-items-center">
    <ImageMod src={logo} alt={`${title} icon`} class="size-7" />
  </div>
  <h3 class="text-h5-sm font-bold font-secondary">{title}</h3>
  <div class="w-px h-11 my-4 bg-text-dark/30"></div>
  <p>
    {content}
  </p>
</div>
